<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    android:fitsSystemWindows="false">

    <!-- WebView ZUERST - damit der Admin-Trigger darüber liegt -->
    <WebView
        android:id="@+id/webView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@+id/bookmarkBarScroll"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <HorizontalScrollView
        android:id="@+id/bookmarkBarScroll"
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:background="#1A1A1A"
        android:scrollbars="none"
        android:elevation="4dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/adminTrigger">

        <LinearLayout
            android:id="@+id/bookmarkBar"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:padding="4dp"
            android:visibility="gone" />
    </HorizontalScrollView>

    <!-- Admin-Trigger ZULETZT - liegt damit über WebView und Bookmarks -->
    <View
        android:id="@+id/adminTrigger"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:background="#664DA3FF"
        android:elevation="20dp"
        android:clickable="true"
        android:focusable="true"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:contentDescription="@string/admin_trigger_desc" />

</androidx.constraintlayout.widget.ConstraintLayout>
