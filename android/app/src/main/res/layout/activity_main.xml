<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    android:fitsSystemWindows="false">

    <com.kiosk.mda.KioskWebView
        android:id="@+id/webView"
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@+id/bookmarkBarScroll"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <HorizontalScrollView
        android:id="@+id/bookmarkBarScroll"
        android:layout_width="0dp"
        android:layout_height="48dp"
        android:background="#1A1A1A"
        android:scrollbars="none"
        android:elevation="4dp"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:id="@+id/bookmarkBar"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:padding="4dp" />
    </HorizontalScrollView>

    <!-- Unsichtbare Touch-Flaeche fuer Triple-Tap / Long-Press -->
    <View
        android:id="@+id/adminTrigger"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:elevation="20dp"
        android:clickable="true"
        android:focusable="true"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:contentDescription="@string/admin_trigger_desc" />

    <!-- Kleiner sichtbarer Indikator-Punkt -->
    <View
        android:id="@+id/adminDot"
        android:layout_width="10dp"
        android:layout_height="10dp"
        android:layout_marginTop="14dp"
        android:layout_marginEnd="14dp"
        android:elevation="21dp"
        android:background="@drawable/admin_dot"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- OSK-Toggle-Button bottom-right (sichtbar/unsichtbar gesteuert via Config) -->
    <ImageButton
        android:id="@+id/oskToggle"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        android:padding="10dp"
        android:elevation="20dp"
        android:background="@drawable/osk_button_bg"
        android:src="@drawable/ic_keyboard_off"
        android:scaleType="fitCenter"
        android:contentDescription="@string/osk_toggle_desc"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
